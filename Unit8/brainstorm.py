import pygame

pygame.init()
clock = pygame.time.Clock()
while True:
    clock.tick(60)
    # begin screen
    # set up interactive background
    # make a sprite
    # allow users to interact with the sprite
    pygame.display.update()